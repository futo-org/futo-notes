# Local note engine rewrite

Starting commit: `8822da1abd202d907a0fa5410f5aba41b9ad8617` (current `origin/main` on 2026-07-13).

This is the single canonical record for the observable-contract rewrite of local note storage, folders, search coordination, desktop/native projections, migrations, and durability. Sync, crypto, editor behavior, shell/session/sync-manager internals, and UI redesign are outside the cut line.

## Baseline

- Rust workspace: 569 passed, 35 ignored. The first sandboxed run failed only because mock servers could not bind localhost; the unrestricted rerun was green.
- Browser acceptance: 419 passed.
- Editor rule/conformance package: 337 passed.
- TypeScript full suite: 893 passed, 10 skipped, 6 pre-existing crash-handler failures caused by this machine's Node localStorage runtime; no scoped note/folder/search failure was reported.
- Android: FFI build and JVM suite green (see Gradle XML executed tests across the whole native test target).
- iOS: both arm64 FFI libraries and Swift bindings build; `just build-ios-native` then fails because the generic simulator link requests x86_64 while the generated XCFramework contains arm64 only. This is a pre-existing baseline defect to fix as part of adapter verification.

## Behavioral cut line

### Preserve

- The vault is a visible tree of durable Markdown files: a note ID is its relative path without the final lowercase `.md`; the filename leaf is the title verbatim.
- Atomic content replacement, exact bytes, modification-time semantics, safe relative paths, collision suffixing, case/Unicode-only renames, conditional background flushes, hidden-file exclusion, and non-destructive folder deletion.
- Folder rename/move and note rename/move rewrite resolvable wikilinks without touching ambiguous aliases; failures never delete notes that did not move.
- First-run welcome seeding, one-way `.txt` migration, Android vault-location migration, dev/prod root isolation, and full-reset ordering.
- Immediate shell render, background indexing, on-device BM25 behavior, empty-query/recent-note behavior, durable external-edit pickup, desktop trash recovery, and the current Swift/Kotlin list semantics.
- Existing browser, Rust, Android, iOS-build, conformance, and cross-platform shell gates as the acceptance boundary.

### Replace and delete

- Free-function note CRUD spread across `futo-notes-core::files` and `futo-notes-model::crud`, plus shell-owned relink/mutation sequences.
- The parallel Tauri note/folder/search command owners and `legacy_filesystem_commands.rs` compatibility surface.
- Note I/O inside the general TypeScript `PlatformFS`, the `fileSystem.ts` forwarding layer, TS-owned `.txt` migration, MiniSearch persistence/fallback coordination, and per-call search notifications.
- Independent FFI `NoteStore` and `SearchEngine` lifecycles and the duplicated Swift/Kotlin mutation/index/reload choreography.
- Internal tests that assert those private layers, callback orders, fallback selection, mocks, or command shapes.

### New ownership model

- `futo-notes-core::files`: small durable-file primitives and hostile-path rejection needed by local storage and the untouched sync consumer.
- `futo-notes-model`: pure filename, tag, preview, image, and wikilink decisions only.
- One shared Rust local-note store: owns vault bootstrap/migrations, snapshots, CRUD/folder transactions, relinking, modification results, and the single search index lifecycle.
- Tauri and UniFFI: transport projections of that store. The desktop projection adds OS trash and watcher event delivery; Swift/Kotlin/TypeScript own only reactive presentation state and background-thread hops.
- Recovery boundary: each mutation computes affected paths first, publishes a pre-write hook for desktop watcher suppression, performs atomic/recoverable disk work, updates the index once, and returns one canonical mutation result to every shell.

## Safety invariants

- No user-controlled path can escape the vault, name the vault root for deletion, exceed the shared depth/name limits, or create a cross-platform-illegal path.
- A content write either replaces the target atomically or leaves the previous bytes recoverable; temporary files never become visible notes.
- Rename collision handling never overwrites a distinct file, including case-only and Unicode-normalization collisions on case-sensitive hosts.
- Conditional flush never resurrects an already-missing note and never overwrites content that changed after the editor snapshot.
- Folder deletion removes the directory only after every contained note has moved and its resolvable backlinks have been rewritten.
- Watcher suppression is registered before mutation, is one-shot, and consumes both sides of a rename atomically.
- Search is never an independent source of truth: startup reconciliation and every committed mutation derive it from the Markdown tree.
- Debug builds and storage migrations never silently repoint or overwrite a production vault.

## Implemented result

- Added `futo-notes-store` as the single durable owner of bootstrap/migration, snapshots, note and folder transactions, collision outcomes, backlink rewrites, conditional writes, reset, watcher notifications, and the one search lifecycle.
- Rebuilt `futo-notes-core::files` as a compact hostile-path and durable-file primitive layer. The retained tests cover contracts and adversarial boundaries rather than reproducing the deleted implementation structure.
- Reduced `futo-notes-model` to pure note rules. Its filesystem CRUD owner, parity scaffolding, relink integration suite, and scan benchmark were deleted.
- Replaced four Tauri note/folder/search/filesystem command owners with one thin `local_notes` projection. All 33 registered desktop commands are now reachable; no dead-command allowlist remains.
- Replaced TypeScript note filesystem and MiniSearch ownership with a platform-neutral local-store contract, a Tauri transport shim, and one cache-mutation seam. The only non-Rust implementation is an explicitly browser-only development/test harness.
- Replaced separate native note/search owners with one UniFFI `NoteStore`; Swift and Kotlin now consume the same snapshot and mutation records. The obsolete Swift `SearchService` was deleted and substring search fallbacks were removed from every production shell.
- Fixed the pre-existing iOS simulator packaging defect by building both simulator architectures and combining them into one universal XCFramework slice (`arm64` + `x86_64`).
- Updated the architecture/spec documents and repository drift, reachability, platform-discipline, and debt registries to describe the new owner instead of preserving deleted APIs.

The 481-row ledger below is the pre-implementation translation and disposition record. Its “planned evidence” cells intentionally preserve that review decision; the executed evidence is recorded in the final verification matrix.

## Legacy-test ledger

The inventory contains **481** reviewed legacy tests: 106 Rust durable files, 76 Rust note model, 9 Rust search, 32 Desktop adapter, 218 TypeScript coordination, and 40 tests adjacent to the Android note-store adapter. Dispositions: 187 Fast, 68 Acceptance, 163 Core, 45 Obsolete, 5 Follow-up, and 13 Excluded editor-lifecycle tests. The count was generated from `cargo test -- --list`, `vitest list`, and the Android JUnit XML before production edits. Keeping excluded adjacent tests in the ledger makes the cut line auditable without pulling editor behavior into the rewrite.

| Removed or reviewed legacy test | Plain-English promise | Disposition | Planned new evidence |
| --- | --- | --- | --- |
| `files::tests::appdata_path_accepts_valid` | At the durable vault boundary, app-data path accepts valid. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::appdata_path_rejects_traversal` | At the durable vault boundary, app-data path rejects traversal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_creates_file` | At the durable vault boundary, atomic write creates file. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_empty_content` | At the durable vault boundary, atomic write empty content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_mixed_line_endings` | At the durable vault boundary, atomic write mixed line endings. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_nested_nonexistent_dirs` | At the durable vault boundary, atomic write nested nonexistent dirs. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_null_bytes_in_content` | At the durable vault boundary, atomic write null bytes in content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_overwrites` | At the durable vault boundary, atomic write overwrites. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_unicode_content` | At the durable vault boundary, atomic write unicode content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::atomic_write_very_long_lines` | At the durable vault boundary, atomic write very long lines. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::blob_base64_roundtrip_preserves_non_utf8_bytes` | At the durable vault boundary, blob base64 roundtrip preserves non utf8 bytes. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_accepts_clean_syncable_names` | At the durable vault boundary, incoming accepts clean syncable names. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_heal_is_idempotent` | At the durable vault boundary, incoming heal is idempotent. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_heals_creatable_but_unsyncable_names` | At the durable vault boundary, incoming heals creatable but unsyncable names. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_ignores_non_syncable` | At the durable vault boundary, incoming ignores non syncable. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_length_gate_is_name_max_bytes_only` | At the durable vault boundary, incoming length gate is name max bytes only. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::incoming_rejects_structurally_unsafe` | At the durable vault boundary, incoming rejects structurally unsafe. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::mtime_round_trip` | At the durable vault boundary, modification time round trip. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::note_id_bare_id_no_extension` | At the durable vault boundary, note ID bare ID no extension. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_case_sensitive_extension` | At the durable vault boundary, note ID case sensitive extension. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_case_sensitivity` | At the durable vault boundary, note ID case sensitivity. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_dots_in_title` | At the durable vault boundary, note ID dots in title. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_double_md_suffix` | At the durable vault boundary, note ID double .md suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_empty` | At the durable vault boundary, note ID empty. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_from_double_md_extension` | At the durable vault boundary, note ID from double .md extension. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_from_just_md` | At the durable vault boundary, note ID from just .md. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_from_md_in_middle` | At the durable vault boundary, note ID from .md in middle. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_from_relative_path_works` | At the durable vault boundary, note ID from relative path works. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_md_as_part_of_title` | At the durable vault boundary, note ID .md as part of title. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_non_md` | At the durable vault boundary, note ID non .md. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_triple_md_suffix` | At the durable vault boundary, note ID triple .md suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_unicode_and_emoji` | At the durable vault boundary, note ID unicode and emoji. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_unicode_filename` | At the durable vault boundary, note ID unicode filename. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::note_id_valid` | At the durable vault boundary, note ID valid. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::rename_through_temp_case_only` | At the durable vault boundary, rename through temp case only. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_appdata_rejects_traversal_variants` | At the durable vault boundary, safe app-data rejects traversal variants. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_appdata_rejects_windows_absolute` | At the durable vault boundary, safe app-data rejects windows absolute. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_accepts_dots_in_middle` | At the durable vault boundary, safe ID accepts dots in middle. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_accepts_hidden_files` | At the durable vault boundary, safe ID accepts hidden files. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_accepts_path_components` | At the durable vault boundary, safe ID accepts path components. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_accepts_valid` | At the durable vault boundary, safe ID accepts valid. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_empty` | At the durable vault boundary, safe ID rejects empty. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_excessive_depth` | At the durable vault boundary, safe ID rejects excessive depth. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_forbidden_chars` | At the durable vault boundary, safe ID rejects forbidden chars. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_null_bytes` | At the durable vault boundary, safe ID rejects null bytes. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_path_traversal_variants` | At the durable vault boundary, safe ID rejects path traversal variants. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_trailing_spaces_with_forbidden` | At the durable vault boundary, safe ID rejects trailing spaces with forbidden. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_id_rejects_traversal` | At the durable vault boundary, safe ID rejects traversal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_note_path_handles_nested_id` | At the durable vault boundary, safe note path handles nested ID. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::safe_note_path_preserves_interior_dots` | At the durable vault boundary, safe note path preserves interior dots. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::sanitize_cjk_characters` | At the durable vault boundary, sanitize cjk characters. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_control_characters_only` | At the durable vault boundary, sanitize control characters only. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_de_reserves_windows_device_names` | At the durable vault boundary, sanitize de reserves windows device names. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_emoji_title` | At the durable vault boundary, sanitize emoji title. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_fallback_on_all_dots` | At the durable vault boundary, sanitize fallback on all dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_fallback_on_empty` | At the durable vault boundary, sanitize fallback on empty. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_mixed_control_and_text` | At the durable vault boundary, sanitize mixed control and text. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_mixed_scripts` | At the durable vault boundary, sanitize mixed scripts. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_mixed_valid_invalid` | At the durable vault boundary, sanitize mixed valid invalid. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_only_dots` | At the durable vault boundary, sanitize only dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_only_forbidden_chars` | At the durable vault boundary, sanitize only forbidden chars. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_only_whitespace` | At the durable vault boundary, sanitize only whitespace. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_output_always_passes_ensure_safe_wide_range` | At the durable vault boundary, sanitize output always passes ensure safe wide range. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_preserves_normal_text` | At the durable vault boundary, sanitize preserves normal text. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_rtl_text` | At the durable vault boundary, sanitize rtl text. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_strips_forbidden` | At the durable vault boundary, sanitize strips forbidden. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_strips_leading_and_trailing_dots` | At the durable vault boundary, sanitize strips leading and trailing dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_title_is_idempotent` | At the durable vault boundary, sanitize title is idempotent. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_trims_whitespace` | At the durable vault boundary, sanitize trims whitespace. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_very_long_title_250_chars` | At the durable vault boundary, sanitize very long title 250 chars. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitize_zero_width_joiners` | At the durable vault boundary, sanitize zero width joiners. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::sanitized_output_passes_safe_id` | At the durable vault boundary, sanitized output passes safe ID. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::unique_id_100_collisions` | At the durable vault boundary, unique ID 100 collisions. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::unique_id_collision_increments` | At the durable vault boundary, unique ID collision increments. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::unique_id_exclude_matches_candidate` | At the durable vault boundary, unique ID exclude matches candidate. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::unique_id_exclude_self` | At the durable vault boundary, unique ID exclude self. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::unique_id_no_collision` | At the durable vault boundary, unique ID no collision. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_clean_title` | At the durable vault boundary, validate clean title. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_empty` | At the durable vault boundary, validate empty. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_forbidden_chars` | At the durable vault boundary, validate forbidden chars. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_leading_and_trailing_dots` | At the durable vault boundary, validate leading and trailing dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_leading_dots` | At the durable vault boundary, validate leading dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_long_with_forbidden` | At the durable vault boundary, validate long with forbidden. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_multiple_issues` | At the durable vault boundary, validate multiple issues. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_tab_only` | At the durable vault boundary, validate tab only. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_title_exactly_max_length` | At the durable vault boundary, validate title exactly max length. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_title_one_over_max` | At the durable vault boundary, validate title one over max. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_title_one_under_max` | At the durable vault boundary, validate title one under max. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_title_with_newlines` | At the durable vault boundary, validate title with newlines. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_too_long` | At the durable vault boundary, validate too long. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_trailing_dots` | At the durable vault boundary, validate trailing dots. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::validate_whitespace_only` | At the durable vault boundary, validate whitespace only. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::windows_reserved_names` | At the durable vault boundary, windows reserved names. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `files::tests::write_atomic_text_basic_roundtrip` | At the durable vault boundary, write atomic text basic roundtrip. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::write_atomic_text_overwrites_case_variant` | At the durable vault boundary, write atomic text overwrites case variant. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `files::tests::write_base64_as_blob_rejects_invalid_base64` | At the durable vault boundary, write base64 as blob rejects invalid base64. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `appdata_traversal_comprehensive` | At the durable vault boundary, app-data traversal comprehensive. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `atomic_write_hash_verify_unicode` | At the durable vault boundary, atomic write hash verify unicode. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `bug_write_atomic_text_long_filename_exceeds_fs_limit` | At the durable vault boundary, bug write atomic text long filename exceeds fs limit. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `full_note_lifecycle_adversarial_titles` | At the durable vault boundary, full note lifecycle adversarial titles. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `note_id_from_filename_adversarial` | At the durable vault boundary, note ID from filename adversarial. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `safe_note_path_traversal_blocked` | At the durable vault boundary, safe note path traversal blocked. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `safe_note_path_valid_ids` | At the durable vault boundary, safe note path valid ids. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `sanitize_is_idempotent` | At the durable vault boundary, sanitize is idempotent. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `unique_id_stress_200_collisions` | At the durable vault boundary, unique ID stress 200 collisions. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `validate_title_is_readonly` | At the durable vault boundary, validate title is readonly. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags::tag_scan_tests::first_char_must_be_letter` | At the shared note-domain boundary, first char must be letter. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags::tag_scan_tests::large_note_extracts_fast_and_correct` | At the shared note-domain boundary, large note extracts fast and correct. | **Follow-up** | Named benchmark or fault-injection seam in the follow-up queue. |
| `tags::tag_scan_tests::length_cap_50` | At the shared note-domain boundary, length cap 50. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags::tag_scan_tests::matches_basic_and_adjacent` | At the shared note-domain boundary, matches basic and adjacent. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags::tag_scan_tests::terminators_and_punctuation` | At the shared note-domain boundary, terminators and punctuation. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags::tag_scan_tests::unicode_whitespace_boundary` | At the shared note-domain boundary, unicode whitespace boundary. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_basic_and_offsets` | At the shared note-domain boundary, find basic and offsets. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_handles_unicode_targets` | At the shared note-domain boundary, find handles unicode targets. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_keeps_pipe_alias_in_target` | At the shared note-domain boundary, find keeps pipe alias in target. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_recovers_after_failed_match` | At the shared note-domain boundary, find recovers after failed match. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_rejects_newline_and_empty` | At the shared note-domain boundary, find rejects newline and empty. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::find_stops_at_first_close_and_tolerates_inner_open` | At the shared note-domain boundary, find stops at first close and tolerates inner open. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::resolve_exact_bare_and_ambiguous` | At the shared note-domain boundary, resolve exact bare and ambiguous. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::resolve_unique_path_suffix` | At the shared note-domain boundary, resolve unique path suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::rewrite_full_path_and_legacy_bare` | At the shared note-domain boundary, rewrite full path and legacy bare. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::rewrite_is_text_level_like_ts` | At the shared note-domain boundary, rewrite is text level like ts. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::rewrite_leaves_alias_and_ambiguous_links_alone` | At the shared note-domain boundary, rewrite leaves alias and ambiguous links alone. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::suffix_falls_back_to_full_id_on_total_collision` | At the shared note-domain boundary, suffix falls back to full ID on total collision. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks::tests::suffix_grows_until_unique` | At the shared note-domain boundary, suffix grows until unique. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `constants_conformance` | At the shared note-domain boundary, constants conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `filename_conformance` | At the shared note-domain boundary, filename conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `image_conformance` | At the shared note-domain boundary, image conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `preview_conformance` | At the shared note-domain boundary, preview conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `tags_conformance` | At the shared note-domain boundary, tags conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `wikilinks_conformance` | At the shared note-domain boundary, wikilinks conformance. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `case_only_rename_in_folder_and_move` | At the shared note-domain boundary, case only rename in folder and move. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `case_only_rename_keeps_requested_case` | At the shared note-domain boundary, case only rename keeps requested case. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `create_folder_makes_dirs` | At the shared note-domain boundary, create folder makes dirs. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `create_note_resolves_collisions` | At the shared note-domain boundary, create note resolves collisions. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `crud_rejects_path_traversal` | At the shared note-domain boundary, crud rejects path traversal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_folder_removes_tree_and_spares_siblings` | At the shared note-domain boundary, delete folder removes tree and spares siblings. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_is_idempotent_and_prunes_empty_folder` | At the shared note-domain boundary, delete is idempotent and prunes empty folder. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_of_missing_note_still_prunes_safely` | At the shared note-domain boundary, delete of missing note still prunes safely. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_prune_stops_at_ancestors_with_remaining_content` | At the shared note-domain boundary, delete prune stops at ancestors with remaining content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_prunes_nested_empty_ancestors_up_to_root` | At the shared note-domain boundary, delete prunes nested empty ancestors up to root. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `emoji_round_trip` | At the shared note-domain boundary, emoji round trip. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `id_helpers` | At the shared note-domain boundary, iD helpers. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `identical_rename_is_noop` | At the shared note-domain boundary, identical rename is noop. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `make_preview_collapses_whitespace_and_caps` | At the shared note-domain boundary, make preview collapses whitespace and caps. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `make_rich_preview_drops_tables_fences_and_caps_lines` | At the shared note-domain boundary, make rich preview drops tables fences and caps lines. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `make_rich_preview_strips_block_markdown_and_keeps_inline` | At the shared note-domain boundary, make rich preview strips block markdown and keeps inline. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `note_tags_lowercase_dedup_no_hash` | At the shared note-domain boundary, note tags lowercase dedup no hash. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `rename_and_move_behavior` | At the shared note-domain boundary, rename and move behavior. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `rename_onto_distinct_case_variant_must_not_clobber` | At the shared note-domain boundary, rename onto distinct case variant must not clobber. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `scan_folders_surfaces_all_ancestors` | At the shared note-domain boundary, scan folders surfaces all ancestors. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `scan_folders_surfaces_empty_dirs` | At the shared note-domain boundary, scan folders surfaces empty dirs. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `scan_skips_hidden` | At the shared note-domain boundary, scan skips hidden. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `scan_sorts_by_mtime_descending` | At the shared note-domain boundary, scan sorts by modification time descending. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `seed_if_empty_writes_one_welcome_note_then_is_idempotent` | At the shared note-domain boundary, seed if empty writes one welcome note then is idempotent. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `wikilink_extraction` | At the shared note-domain boundary, wikilink extraction. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `write_if_unchanged_never_resurrects_missing_note` | At the shared note-domain boundary, write if unchanged never resurrects missing note. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `write_if_unchanged_preserves_content_changed_since_snapshot` | At the shared note-domain boundary, write if unchanged preserves content changed since snapshot. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `write_if_unchanged_rejects_path_traversal` | At the shared note-domain boundary, write if unchanged rejects path traversal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `write_if_unchanged_writes_when_base_matches` | At the shared note-domain boundary, write if unchanged writes when base matches. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_folder_moves_notes_up_preserving_substructure` | At the shared note-domain boundary, delete folder moves notes up preserving substructure. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_folder_rejects_root_and_traversal` | At the shared note-domain boundary, delete folder rejects root and traversal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_folder_relinks_wikilinks_to_moved_notes` | At the shared note-domain boundary, delete folder relinks wikilinks to moved notes. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `delete_folder_suffixes_on_collision` | At the shared note-domain boundary, delete folder suffixes on collision. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_missing_folder_is_a_noop` | At the shared note-domain boundary, delete missing folder is a noop. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `delete_root_level_folder_moves_notes_to_vault_root` | At the shared note-domain boundary, delete root level folder moves notes to vault root. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_is_text_level_like_ts_so_code_fences_are_rewritten_too` | At the shared note-domain boundary, relink is text level like ts so code fences are rewritten too. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_leaves_ambiguous_and_alias_links_untouched` | At the shared note-domain boundary, relink leaves ambiguous and alias links untouched. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_rewrites_pointing_notes_and_skips_the_rest` | At the shared note-domain boundary, relink rewrites pointing notes and skips the rest. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_rewrites_self_link_bare_leaf` | At the shared note-domain boundary, relink rewrites self link bare leaf. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_rewrites_self_link_full_path_form` | At the shared note-domain boundary, relink rewrites self link full path form. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_rewrites_self_link_on_folder_move` | At the shared note-domain boundary, relink rewrites self link on folder move. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `relink_same_id_is_a_noop` | At the shared note-domain boundary, relink same ID is a noop. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `bench_per_note` | At the shared note-domain boundary, bench per note. | **Follow-up** | Named benchmark or fault-injection seam in the follow-up queue. |
| `bench_scan_5k` | At the shared note-domain boundary, bench scan 5k. | **Follow-up** | Named benchmark or fault-injection seam in the follow-up queue. |
| `bench_tag_scaling` | At the shared note-domain boundary, bench tag scaling. | **Follow-up** | Named benchmark or fault-injection seam in the follow-up queue. |
| `note_tags_no_code_regions_matches_extract` | At the shared note-domain boundary, note tags no code regions matches extract. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `parallel_scan_matches_serial_on_large_fixture` | At the shared note-domain boundary, parallel scan matches serial on large fixture. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `preview_crlf_vs_bare_cr_and_no_multispace_collapse` | At the shared note-domain boundary, preview crlf vs bare cr and no multispace collapse. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `preview_internal_ws_within_budget_is_kept_when_content_follows` | At the shared note-domain boundary, preview internal ws within budget is kept when content follows. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `preview_trailing_ws_dropped_when_no_content_follows` | At the shared note-domain boundary, preview trailing ws dropped when no content follows. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `preview_unicode_take_is_by_code_point` | At the shared note-domain boundary, preview unicode take is by code point. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `indexer::tests::measure_warm_reconcile_5k` | At the on-device search boundary, measure warm reconcile 5k. | **Follow-up** | Named benchmark or fault-injection seam in the follow-up queue. |
| `indexer::tests::warm_reconcile_only_reindexes_changed_and_handles_deletion` | At the on-device search boundary, warm reconcile only reindexes changed and handles deletion. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `indexer::tests::warm_reconcile_skips_unchanged_vault` | At the on-device search boundary, warm reconcile skips unchanged vault. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tantivy_indices::tests::bm25_note_mtimes_returns_committed_mtimes` | At the on-device search boundary, bM25 note mtimes returns committed mtimes. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tantivy_indices::tests::delete_note_removes_from_subsequent_list` | At the on-device search boundary, delete note removes from subsequent list. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tantivy_indices::tests::list_bm25_note_ids_returns_committed_notes_only` | At the on-device search boundary, list BM25 note ids returns committed notes only. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tantivy_indices::tests::open_removes_old_splade_sidecars` | At the on-device search boundary, open removes old splade sidecars. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tests::bm25_engine_indexes_and_queries` | At the on-device search boundary, bM25 engine indexes and queries. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `tests::hyphenated_query_is_an_adjacent_phrase` | At the on-device search boundary, hyphenated query is an adjacent phrase. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::events_are_classified_without_metadata_noise` | The desktop shell must ensure that events are classified without metadata noise. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::expired_suppression_is_not_consumed` | The desktop shell must ensure that expired suppression is not consumed. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::partial_rename_registration_consumes_neither_path` | The desktop shell must ensure that partial rename registration consumes neither path. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::path_resolution_accepts_raw_and_canonical_root_spellings` | The desktop shell must ensure that path resolution accepts raw and canonical root spellings. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::paths_are_normalized_and_hidden_paths_are_rejected` | The desktop shell must ensure that paths are normalized and hidden paths are rejected. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::rename_consumes_both_paths_atomically` | The desktop shell must ensure that rename consumes both paths atomically. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::suppression_is_one_shot` | The desktop shell must ensure that suppression is one shot. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `filesystem_watcher::tests::suppression_window_matches_cross_language_constant` | The desktop shell must ensure that suppression window matches cross language constant. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `folder_commands::tests::create_folder_keeps_the_canonical_sanitization_contract` | The desktop shell must ensure that create folder keeps the canonical sanitization contract. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `folder_commands::tests::delete_suppresses_every_note_in_the_subtree` | The desktop shell must ensure that delete suppresses every note in the subtree. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `folder_commands::tests::rename_moves_subtree_and_suppresses_every_note_path` | The desktop shell must ensure that rename moves subtree and suppresses every note path. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `folder_commands::tests::rename_rejects_an_existing_target_without_mutating_source` | The desktop shell must ensure that rename rejects an existing target without mutating source. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `image_commands::tests::extension_allowlist_is_case_insensitive_and_traversal_safe` | The desktop shell must ensure that extension allowlist is case insensitive and traversal safe. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `image_commands::tests::image_write_returns_a_vault_relative_filename` | The desktop shell must ensure that image write returns a vault relative filename. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `legacy_filesystem_commands::tests::legacy_folder_listing_is_sorted_and_skips_hidden_directories` | The desktop shell must ensure that legacy folder listing is sorted and skips hidden directories. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `legacy_filesystem_commands::tests::legacy_metadata_excludes_non_notes_and_hidden_directories` | The desktop shell must ensure that legacy metadata excludes non notes and hidden directories. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `legacy_filesystem_commands::tests::legacy_metadata_recurses_and_sorts_by_mtime_descending` | The desktop shell must ensure that legacy metadata recurses and sorts by modification time descending. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `legacy_filesystem_commands::tests::legacy_metadata_returns_empty_for_a_missing_root` | The desktop shell must ensure that legacy metadata returns empty for a missing root. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `legacy_filesystem_commands::tests::legacy_metadata_shape_keeps_filename_and_size` | The desktop shell must ensure that legacy metadata shape keeps filename and size. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `note_commands::tests::create_collision_suppresses_the_resolved_path` | The desktop shell must ensure that create collision suppresses the resolved path. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::create_write_read_scan_roundtrip` | The desktop shell must ensure that create write read scan roundtrip. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::create_writes_content_atomically` | The desktop shell must ensure that create writes content atomically. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::delete_is_idempotent_on_missing` | The desktop shell must ensure that delete is idempotent on missing. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::delete_to_trash_prunes_empty_parents` | The desktop shell must ensure that delete to trash prunes empty parents. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `note_commands::tests::move_into_folder` | The desktop shell must ensure that move into folder. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::rename_collision_resolves_and_suppresses_final` | The desktop shell must ensure that rename collision resolves and suppresses final. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::rename_suppresses_old_and_new` | The desktop shell must ensure that rename suppresses old and new. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::seed_is_idempotent_and_suppresses_the_welcome_write` | The desktop shell must ensure that seed is idempotent and suppresses the welcome write. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `note_commands::tests::write_honors_mtime_override` | The desktop shell must ensure that write honors modification time override. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `search_commands::tests::search_state_starts_uninitialized` | The desktop shell must ensure that search state starts uninitialized. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `system_trash::tests::hard_delete_handles_files_and_directories` | The desktop shell must ensure that hard delete handles files and directories. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `vault_location::tests::environment_data_dir_tracks_override` | The desktop shell must ensure that environment data dir tracks override. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notes.test.ts > initNotes > rebuilds cache from files on disk` | The web/desktop shell must ensure that rebuilds cache from files on disk. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > initNotes > is idempotent` | The web/desktop shell must ensure that is idempotent. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > deleteAllNotes > stops live sync and disconnects before wiping the vault` | The web/desktop shell must ensure that stops live sync and disconnects before wiping the vault. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > createNote > writes file and adds to cache` | The web/desktop shell must ensure that writes file and adds to cache. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > createNote > generates unique id on conflict` | The web/desktop shell must ensure that generates unique ID on conflict. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > createNote > a failed create leaves no orphan and a retry keeps the intended id` | The web/desktop shell must ensure that a failed create leaves no orphan and a retry keeps the intended ID. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > updateNote > updates content and cache` | The web/desktop shell must ensure that updates content and cache. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > updateNote > handles rename (deletes old, writes new)` | The web/desktop shell must ensure that handles rename (deletes old, writes new). | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > updateNote > a failed write during a title-rename leaves the note recoverable at the original id` | The web/desktop shell must ensure that a failed write during a title-rename leaves the note recoverable at the original ID. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > updateNote > a rename whose source was externally deleted recreates it from the draft and converges` | The web/desktop shell must ensure that a rename whose source was externally deleted recreates it from the draft and converges. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > deleteNote > removes file and cache` | The web/desktop shell must ensure that removes file and cache. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > deleteNote > routes through deleteNoteToTrash instead of the hard-delete path when the platform provides it` | The web/desktop shell must ensure that routes through deleteNoteToTrash instead of the hard-delete path when the platform provides it. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > getAllNotes / getNoteById > returns notes sorted by modification time descending` | The web/desktop shell must ensure that returns notes sorted by modification time descending. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > getAllNotes / getNoteById > getNoteById finds by id` | The web/desktop shell must ensure that getNoteById finds by ID. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > getAllNotes / getNoteById > getNoteById returns undefined for missing` | The web/desktop shell must ensure that getNoteById returns undefined for missing. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > getAllNotes / getNoteById > newly created note appears at position 0` | The web/desktop shell must ensure that newly created note appears at position 0. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > getAllNotes / getNoteById > editing a note not at position 0 moves it to position 0` | The web/desktop shell must ensure that editing a note not at position 0 moves it to position 0. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > returns all notes for empty query` | The web/desktop shell must ensure that returns all notes for empty query. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > filters for non-empty query` | The web/desktop shell must ensure that filters for non-empty query. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > matches against note id` | The web/desktop shell must ensure that matches against note ID. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > returns empty results when index is populated but query has no matches` | The web/desktop shell must ensure that returns empty results when index is populated but query has no matches. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > finds existing notes by body content after startup (no creations yet)` | The web/desktop shell must ensure that finds existing notes by body content after startup (no creations yet). | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > removes deleted notes from search index` | The web/desktop shell must ensure that removes deleted notes from search index. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > search > removes renamed id from search index and finds under new id` | The web/desktop shell must ensure that removes renamed ID from search index and finds under new ID. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > handleExternalFileChange (F18: incremental, no full rescan) > updates a single changed note without re-scanning the whole vault` | The web/desktop shell must ensure that updates a single changed note without re-scanning the whole vault. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > handleExternalFileChange (F18: incremental, no full rescan) > removes a deleted note incrementally (no full rescan) and drops it from search` | The web/desktop shell must ensure that removes a deleted note incrementally (no full rescan) and drops it from search. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > handleExternalFileChange (F18: incremental, no full rescan) > adds a brand-new note that appeared externally, indexing it for search` | The web/desktop shell must ensure that adds a brand-new note that appeared externally, indexing it for search. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > handleExternalFileChange (F18: incremental, no full rescan) > re-derives the canonical preview/tags shape on an external change` | The web/desktop shell must ensure that re-derives the canonical preview/tags shape on an external change. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > handleExternalFileChange (F18: incremental, no full rescan) > falls back to a full rescan when the incremental read throws` | The web/desktop shell must ensure that falls back to a full rescan when the incremental read throws. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > treats nested files as path-IDs end-to-end` | The web/desktop shell must ensure that treats nested files as path-IDs end-to-end. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNote rewrites wikilinks in other notes` | The web/desktop shell must ensure that moveNote rewrites wikilinks in other notes. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > updateNote rename rewrites self-referencing wikilinks in the renamed note` | The web/desktop shell must ensure that updateNote rename rewrites self-referencing wikilinks in the renamed note. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNote rewrites self-referencing wikilinks in the moved note` | The web/desktop shell must ensure that moveNote rewrites self-referencing wikilinks in the moved note. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNote rewrites legacy bare-filename wikilinks when unique` | The web/desktop shell must ensure that moveNote rewrites legacy bare-filename wikilinks when unique. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNote does not rewrite ambiguous bare-filename wikilinks` | The web/desktop shell must ensure that moveNote does not rewrite ambiguous bare-filename wikilinks. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNote suffixes the incoming file when target ID already exists` | The web/desktop shell must ensure that moveNote suffixes the incoming file when target ID already exists. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNotesUnderPrefix relocates every nested note` | The web/desktop shell must ensure that moveNotesUnderPrefix relocates every nested note. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/notes.test.ts > folder support: path-as-ID > moveNotesUnderPrefix is idempotent when fs.renameFolder ran first` | The web/desktop shell must ensure that moveNotesUnderPrefix is idempotent when fs.renameFolder ran first. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > listNoteFiles returns empty initially` | The web/desktop shell must ensure that listNoteFiles returns empty initially. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > writeNote + readNote round-trip` | The web/desktop shell must ensure that writeNote + readNote round-trip. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > writeNote returns mtime` | The web/desktop shell must ensure that writeNote returns modification time. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > listNoteFiles includes written notes` | The web/desktop shell must ensure that listNoteFiles includes written notes. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > noteExists returns true for existing, false for missing` | The web/desktop shell must ensure that noteExists returns true for existing, false for missing. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > deleteNoteFile removes note` | The web/desktop shell must ensure that deleteNoteFile removes note. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > readNote returns "" for nonexistent (never throws)` | The web/desktop shell must ensure that readNote returns "" for nonexistent (never throws). | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > mtime preservation via modifiedAtMs` | The web/desktop shell must ensure that modification time preservation via modifiedAtMs. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > readAppData returns null for missing` | The web/desktop shell must ensure that readAppData returns null for missing. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > writeAppData + readAppData round-trip` | The web/desktop shell must ensure that writeAppData + readAppData round-trip. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > deleteAppData removes file` | The web/desktop shell must ensure that deleteAppData removes file. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > listAppData returns files in subdirectory` | The web/desktop shell must ensure that listAppData returns files in subdirectory. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > listAppData returns empty for nonexistent dir` | The web/desktop shell must ensure that listAppData returns empty for nonexistent dir. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > createNote writes content atomically and returns id + mtime` | The web/desktop shell must ensure that createNote writes content atomically and returns ID + modification time. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > createNote suffixes -2/-3 on collision` | The web/desktop shell must ensure that createNote suffixes -2/-3 on collision. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > renameNote suffixes when the destination is a distinct note` | The web/desktop shell must ensure that renameNote suffixes when the destination is a distinct note. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > renameNote case-only rename matches Rust on a case-sensitive host` | The web/desktop shell must ensure that renameNote case-only rename matches Rust on a case-sensitive host. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > getAppVersion returns a string` | The web/desktop shell must ensure that getAppVersion returns a string. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/platformFS.contract.test.ts > PlatformFS contract: nodeFS > getPlatformName returns a string` | The web/desktop shell must ensure that getPlatformName returns a string. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > creates a new file with correct content` | The web/desktop shell must ensure that creates a new file with correct content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > replaces existing file content atomically` | The web/desktop shell must ensure that replaces existing file content atomically. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > creates parent directory if missing` | The web/desktop shell must ensure that creates parent directory if missing. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > leaves no temp files on success` | The web/desktop shell must ensure that leaves no temp files on success. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > handles concurrent writes to different files` | The web/desktop shell must ensure that handles concurrent writes to different files. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > preserves Unicode content exactly` | The web/desktop shell must ensure that preserves Unicode content exactly. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > handles empty content` | The web/desktop shell must ensure that handles empty content. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > throws on invalid path (no parent)` | The web/desktop shell must ensure that throws on invalid path (no parent). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > cleans up temp file on write failure` | The web/desktop shell must ensure that cleans up temp file on write failure. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > retries once when rename fails with a not-found error (temp yanked)` | The web/desktop shell must ensure that retries once when rename fails with a not-found error (temp yanked). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > handles concurrent writes to the same file without throwing` | The web/desktop shell must ensure that handles concurrent writes to the same file without throwing. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/atomicWrite.test.ts > writeAtomicText > propagates rename failure and cleans up temp` | The web/desktop shell must ensure that propagates rename failure and cleans up temp. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > buildFolderTree > returns flat note list when no folders` | The web/desktop shell must ensure that returns flat note list when no folders. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > buildFolderTree > groups notes under shared parent folder` | The web/desktop shell must ensure that groups notes under shared parent folder. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > buildFolderTree > handles nested paths` | The web/desktop shell must ensure that handles nested paths. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > buildFolderTree > sorts folders alphabetically and folders-before-notes` | The web/desktop shell must ensure that sorts folders alphabetically and folders-before-notes. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > flattenTree > respects open/closed state via isFolderOpen` | The web/desktop shell must ensure that respects open/closed state via isFolderOpen. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > flattenTree > emits an empty placeholder row for an open childless folder` | The web/desktop shell must ensure that emits an empty placeholder row for an open childless folder. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > flattenTree > emits no placeholder for a CLOSED childless folder` | The web/desktop shell must ensure that emits no placeholder for a CLOSED childless folder. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > flattenTree > emits no placeholder for an open folder with children` | The web/desktop shell must ensure that emits no placeholder for an open folder with children. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > flattenTree > indents the placeholder one level under a nested folder` | The web/desktop shell must ensure that indents the placeholder one level under a nested folder. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/folders.test.ts > validateNewFolderName > returns null for a fresh valid name` | The web/desktop shell must ensure that returns null for a fresh valid name. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/folders.test.ts > validateNewFolderName > rejects a case-insensitive sibling duplicate` | The web/desktop shell must ensure that rejects a case-insensitive sibling duplicate. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/folders.test.ts > validateNewFolderName > rejects an empty name` | The web/desktop shell must ensure that rejects an empty name. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/folders.test.ts > validateNewFolderName > rejects names the shared rules refuse (forbidden chars, trailing dots)` | The web/desktop shell must ensure that rejects names the shared rules refuse (forbidden chars, trailing dots). | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/folders.test.ts > validateNewFolderName > rejects names that exceed the folder depth limit` | The web/desktop shell must ensure that rejects names that exceed the folder depth limit. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `src/lib/fileSystem.test.ts > deleteNoteFileToTrash > routes through deleteNoteToTrash and does not call the hard-delete path when the platform implements it` | The web/desktop shell must ensure that routes through deleteNoteToTrash and does not call the hard-delete path when the platform implements it. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/fileSystem.test.ts > deleteNoteFileToTrash > falls back to permanent delete when the platform does not implement deleteNoteToTrash` | The web/desktop shell must ensure that falls back to permanent delete when the platform does not implement deleteNoteToTrash. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/fileSystem.test.ts > deleteNoteFileToTrash > records the write suppression and notifies the search engine on both paths` | The web/desktop shell must ensure that records the write suppression and notifies the search engine on both paths. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > extractHeadings > extracts h1-h6 headings` | The web/desktop shell must ensure that extracts h1-h6 headings. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > extractHeadings > returns empty string for no headings` | The web/desktop shell must ensure that returns empty string for no headings. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > extractHeadings > handles headings with inline formatting` | The web/desktop shell must ensure that handles headings with inline formatting. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > extractHeadings > does not match lines without space after #` | The web/desktop shell must ensure that does not match lines without space after #. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > highlights a single term` | The web/desktop shell must ensure that highlights a single term. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > highlights multiple terms` | The web/desktop shell must ensure that highlights multiple terms. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > merges overlapping ranges` | The web/desktop shell must ensure that merges overlapping ranges. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > returns single unhighlighted segment for no matches` | The web/desktop shell must ensure that returns single unhighlighted segment for no matches. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > handles case-insensitive matching` | The web/desktop shell must ensure that handles case-insensitive matching. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > buildHighlightedSegments > handles no terms` | The web/desktop shell must ensure that handles no terms. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > extractSnippet > centers snippet on first match in body` | The web/desktop shell must ensure that centers snippet on first match in body. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > extractSnippet > falls back to first 120 chars when match is title-only` | The web/desktop shell must ensure that falls back to first 120 chars when match is title-only. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > extractSnippet > handles match at start of body` | The web/desktop shell must ensure that handles match at start of body. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > extractSnippet > handles short body` | The web/desktop shell must ensure that handles short body. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > Unicode snippet boundaries > handles emoji in highlighted text` | The web/desktop shell must ensure that handles emoji in highlighted text. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > Unicode snippet boundaries > handles CJK text in highlighted segments` | The web/desktop shell must ensure that handles CJK text in highlighted segments. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > Unicode snippet boundaries > handles multi-term highlighting across Unicode text` | The web/desktop shell must ensure that handles multi-term highlighting across Unicode text. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > Unicode snippet boundaries > handles combining characters` | The web/desktop shell must ensure that handles combining characters. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchIndex.test.ts > empty search results > returns empty array for empty query` | The web/desktop shell must ensure that returns empty array for empty query. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > empty search results > returns empty array for whitespace-only query` | The web/desktop shell must ensure that returns empty array for whitespace-only query. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > empty search results > returns empty array when no documents match` | The web/desktop shell must ensure that returns empty array when no documents match. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > persistence > round-trips: save and load index` | The web/desktop shell must ensure that round-trips: save and load index. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > persistence > returns false on missing persisted file` | The web/desktop shell must ensure that returns false on missing persisted file. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > persistence > returns false on version mismatch` | The web/desktop shell must ensure that returns false on version mismatch. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchIndex.test.ts > persistence > stored body is accessible after load` | The web/desktop shell must ensure that stored body is accessible after load. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/tauriConfig.test.ts > getConfig > returns default config when no override and no config file` | The web/desktop shell must ensure that returns default config when no override and no config file. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > getConfig > returns custom dir when override is set` | The web/desktop shell must ensure that returns custom dir when override is set. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > getConfig > reads sidebar widths from config file` | The web/desktop shell must ensure that reads sidebar widths from config file. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > getConfig > degrades to defaults when the config read is denied (macOS EPERM)` | The web/desktop shell must ensure that degrades to defaults when the config read is denied (macOS EPERM). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > getConfig > handles invalid JSON in config file gracefully` | The web/desktop shell must ensure that handles invalid JSON in config file gracefully. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > saveConfig > merges sidebarWidth into existing config` | The web/desktop shell must ensure that merges sidebarWidth into existing config. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > saveConfig > can set a width to null` | The web/desktop shell must ensure that can set a width to null. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > saveConfig > persists openFolders alongside other config fields` | The web/desktop shell must ensure that persists openFolders alongside other config fields. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > saveConfig > does not write a partial config when the existing config read is denied` | The web/desktop shell must ensure that does not write a partial config when the existing config read is denied. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > loadOpenFoldersConfig > returns null when the config file has no openFolders entry` | The web/desktop shell must ensure that returns null when the config file has no openFolders entry. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > loadOpenFoldersConfig > returns null when the config file is missing` | The web/desktop shell must ensure that returns null when the config file is missing. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > loadOpenFoldersConfig > returns the persisted folder paths and filters non-strings` | The web/desktop shell must ensure that returns the persisted folder paths and filters non-strings. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > loadOpenFoldersConfig > returns an empty array when openFolders was persisted as []` | The web/desktop shell must ensure that returns an empty array when openFolders was persisted as []. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > setNotesDir > saves override for absolute path` | The web/desktop shell must ensure that saves override for absolute path. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > setNotesDir > clears override when dir is null` | The web/desktop shell must ensure that clears override when dir is null. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriConfig.test.ts > setNotesDir > rejects relative paths` | The web/desktop shell must ensure that rejects relative paths. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notesIndex.test.ts > makePreview > returns short content as-is` | The web/desktop shell must ensure that returns short content as-is. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > replaces newlines with spaces` | The web/desktop shell must ensure that replaces newlines with spaces. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > truncates at 100 characters` | The web/desktop shell must ensure that truncates at 100 characters. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > returns empty string for empty content` | The web/desktop shell must ensure that returns empty string for empty content. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > handles content exactly 100 chars` | The web/desktop shell must ensure that handles content exactly 100 chars. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > collapses CRLF to a single space (not two)` | The web/desktop shell must ensure that collapses CRLF to a single space (not two). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > collapses tabs to spaces` | The web/desktop shell must ensure that collapses tabs to spaces. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > trims leading and trailing whitespace` | The web/desktop shell must ensure that trims leading and trailing whitespace. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > trims leading newlines before truncating` | The web/desktop shell must ensure that trims leading newlines before truncating. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > truncates AFTER collapsing/trimming (100 visible chars)` | The web/desktop shell must ensure that truncates AFTER collapsing/trimming (100 visible chars). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > counts unicode by code point (chars), matching Rust .chars().take()` | The web/desktop shell must ensure that counts unicode by code point (chars), matching Rust .chars().take(). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > makePreview > returns empty string for whitespace-only content` | The web/desktop shell must ensure that returns empty string for whitespace-only content. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notesIndex.test.ts > convertTxtToMd > renames .txt to .md when no collision` | The web/desktop shell must ensure that renames ..txt to ..md when no collision. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notesIndex.test.ts > convertTxtToMd > handles collision: x.txt + x.md -> x (imported).md` | The web/desktop shell must ensure that handles collision: x..txt + x..md -> x (imported)..md. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notesIndex.test.ts > convertTxtToMd > is a no-op when there are no .txt files` | The web/desktop shell must ensure that is a no-op when there are no ..txt files. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notesIndex.test.ts > convertTxtToMd > writes a sentinel that short-circuits subsequent calls` | The web/desktop shell must ensure that writes a sentinel that short-circuits subsequent calls. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > createNote notifies a change for the new note` | The web/desktop shell must ensure that createNote notifies a change for the new note. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > updateNote (content edit) notifies a change` | The web/desktop shell must ensure that updateNote (content edit) notifies a change. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > updateNote (rename) re-keys the old path to the new one and notifies its change` | The web/desktop shell must ensure that updateNote (rename) re-keys the old path to the new one and notifies its change. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > deleteNote notifies an unlink` | The web/desktop shell must ensure that deleteNote notifies an unlink. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > moveNote notifies a rename from old → new path` | The web/desktop shell must ensure that moveNote notifies a rename from old → new path. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/features/search/searchEngineNotify.test.ts > search engine staleness: local mutations notify the Rust engine > wikilink rewrite during a move notifies a change for the rewritten note` | The web/desktop shell must ensure that wikilink rewrite during a move notifies a change for the rewritten note. | **Obsolete** | Retired adapter/fallback/test protocol; no replacement test. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects empty string` | The web/desktop shell must ensure that rejects empty string. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects . and ..` | The web/desktop shell must ensure that rejects . and .. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > accepts forward slashes between valid components (path-as-ID)` | The web/desktop shell must ensure that accepts forward slashes between valid components (path-as-ID). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects backslashes (always invalid)` | The web/desktop shell must ensure that rejects backslashes (always invalid). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects empty / dot / dotdot components` | The web/desktop shell must ensure that rejects empty / dot / dotdot components. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects depth above MAX_FOLDER_DEPTH` | The web/desktop shell must ensure that rejects depth above MAX_FOLDER_DEPTH. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects forbidden characters` | The web/desktop shell must ensure that rejects forbidden characters. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects control characters` | The web/desktop shell must ensure that rejects control characters. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects null bytes at various positions` | The web/desktop shell must ensure that rejects null bytes at various positions. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > accepts valid names` | The web/desktop shell must ensure that accepts valid names. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > accepts whitespace-only IDs (Rust behavior)` | The web/desktop shell must ensure that accepts whitespace-only IDs (Rust behavior). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > accepts unicode and emoji` | The web/desktop shell must ensure that accepts unicode and emoji. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > allows leading/trailing dots (unlike title validation)` | The web/desktop shell must ensure that allows leading/trailing dots (unlike title validation). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > rejects adversarial inputs` | The web/desktop shell must ensure that rejects adversarial inputs. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > ensureSafeNoteId > works correctly when called multiple times (no regex state leak)` | The web/desktop shell must ensure that works correctly when called multiple times (no regex state leak). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeNotePath > returns base/id.md for valid ID` | The web/desktop shell must ensure that returns base/ID..md for valid ID. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeNotePath > throws on invalid ID` | The web/desktop shell must ensure that throws on invalid ID. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeNotePath > builds nested path for path-as-ID` | The web/desktop shell must ensure that builds nested path for path-as-ID. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeAppdataPath > rejects absolute paths` | The web/desktop shell must ensure that rejects absolute paths. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeAppdataPath > rejects .. traversal` | The web/desktop shell must ensure that rejects .. traversal. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeAppdataPath > rejects empty components (double slashes)` | The web/desktop shell must ensure that rejects empty components (double slashes). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeAppdataPath > accepts valid relative paths` | The web/desktop shell must ensure that accepts valid relative paths. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > safeAppdataPath > rejects . components (plugin-fs scope rejects them as forbidden)` | The web/desktop shell must ensure that rejects . components (plugin-fs scope rejects them as forbidden). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > strips .md suffix` | The web/desktop shell must ensure that strips ..md suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > handles nested path filenames` | The web/desktop shell must ensure that handles nested path filenames. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > throws for non-.md files` | The web/desktop shell must ensure that throws for non-..md files. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > throws for .md alone (empty ID)` | The web/desktop shell must ensure that throws for ..md alone (empty ID). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > is case-sensitive (.MD does not match)` | The web/desktop shell must ensure that is case-sensitive (..md does not match). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > strips only one trailing .md` | The web/desktop shell must ensure that strips only one trailing ..md. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > handles unicode and emoji filenames` | The web/desktop shell must ensure that handles unicode and emoji filenames. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > handles dots in titles` | The web/desktop shell must ensure that handles dots in titles. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/pathSafety.test.ts > noteIdFromFilename > handles .md embedded but not at suffix` | The web/desktop shell must ensure that handles ..md embedded but not at suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/notes.fullReset.test.ts > deleteAllNotes (Full reset) durably kills the sync session > drops the connection + stored password BEFORE wiping the vault` | The web/desktop shell must ensure that drops the connection + stored password BEFORE wiping the vault. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notes.fullReset.test.ts > deleteAllNotes (Full reset) durably kills the sync session > pauses and drains in-flight sync before disconnecting` | The web/desktop shell must ensure that pauses and drains in-flight sync before disconnecting. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notes.fullReset.test.ts > deleteAllNotes (Full reset) durably kills the sync session > never re-arms sync while the session is still connected (resume only after disconnect)` | The web/desktop shell must ensure that never re-arms sync while the session is still connected (resume only after disconnect). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notes.fullReset.test.ts > deleteAllNotes (Full reset) durably kills the sync session > a failed wipe still propagates AND un-pauses sync (reset failure must not leave sync dead)` | The web/desktop shell must ensure that a failed wipe still propagates AND un-pauses sync (reset failure must not leave sync dead). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/notes.fullReset.test.ts > deleteAllNotes (Full reset) durably kills the sync session > a failed disconnect aborts the reset without touching the vault` | The web/desktop shell must ensure that a failed disconnect aborts the reset without touching the vault. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/wikilinks.test.ts > noteIdLeaf > returns leaf component` | The web/desktop shell must ensure that returns leaf component. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > shortestUniqueSuffix > returns leaf when leaf is unique` | The web/desktop shell must ensure that returns leaf when leaf is unique. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > shortestUniqueSuffix > extends suffix until unique (spec example)` | The web/desktop shell must ensure that extends suffix until unique (spec example). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > shortestUniqueSuffix > falls back to full ID when no shorter suffix is unique` | The web/desktop shell must ensure that falls back to full ID when no shorter suffix is unique. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > shortestUniqueSuffix > handles single-note vault` | The web/desktop shell must ensure that handles single-note vault. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > resolves exact full path` | The web/desktop shell must ensure that resolves exact full path. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > resolves a unique bare filename (legacy)` | The web/desktop shell must ensure that resolves a unique bare filename (legacy). | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > returns null for ambiguous bare filename` | The web/desktop shell must ensure that returns null for ambiguous bare filename. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > returns null for absent target` | The web/desktop shell must ensure that returns null for absent target. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > resolves a unique multi-component path-suffix` | The web/desktop shell must ensure that resolves a unique multi-component path-suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > returns null for ambiguous multi-component suffix` | The web/desktop shell must ensure that returns null for ambiguous multi-component suffix. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > resolveWikilink > returns null for empty string` | The web/desktop shell must ensure that returns null for empty string. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > findWikilinks > finds simple wikilinks` | The web/desktop shell must ensure that finds simple wikilinks. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > findWikilinks > handles full-path targets` | The web/desktop shell must ensure that handles full-path targets. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > findWikilinks > returns empty for no wikilinks` | The web/desktop shell must ensure that returns empty for no wikilinks. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > rewrites full-path target on rename` | The web/desktop shell must ensure that rewrites full-path target on rename. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > rewrites legacy bare filename when unique` | The web/desktop shell must ensure that rewrites legacy bare filename when unique. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > does not rewrite ambiguous bare filename` | The web/desktop shell must ensure that does not rewrite ambiguous bare filename. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > does not touch unrelated wikilinks` | The web/desktop shell must ensure that does not touch unrelated wikilinks. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > handles multiple occurrences` | The web/desktop shell must ensure that handles multiple occurrences. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > returns text unchanged when no rewrites apply` | The web/desktop shell must ensure that returns text unchanged when no rewrites apply. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/wikilinks.test.ts > rewriteWikilinks > handles a folder rename that moves multiple notes` | The web/desktop shell must ensure that handles a folder rename that moves multiple notes. | **Core** | Canonical Rust or TS↔Rust conformance coverage; no adapter-level duplicate. |
| `src/lib/platform/tauriPaths.test.ts > loadNotesDirOverride > returns override path when set` | The web/desktop shell must ensure that returns override path when set. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > loadNotesDirOverride > returns null when no override` | The web/desktop shell must ensure that returns null when no override. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > saveNotesDirOverride > saves a custom directory` | The web/desktop shell must ensure that saves a custom directory. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > saveNotesDirOverride > clears override by passing null` | The web/desktop shell must ensure that clears override by passing null. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > getDefaultNotesRoot > delegates to Rust (honors FUTO_NOTES_DATA_DIR)` | The web/desktop shell must ensure that delegates to Rust (honors FUTO_NOTES_DATA_DIR). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > getDefaultNotesRoot > returns the Documents/futo-notes path in production` | The web/desktop shell must ensure that returns the Documents/futo-notes path in production. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > getNotesRoot > returns override dir when set and creates it` | The web/desktop shell must ensure that returns override dir when set and creates it. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > getNotesRoot > returns Rust-resolved default dir when no override and creates it` | The web/desktop shell must ensure that returns Rust-resolved default dir when no override and creates it. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > getNotesRoot > honors env-derived root from Rust (e.g. FUTO_NOTES_DATA_DIR for dev/test isolation)` | The web/desktop shell must ensure that honors env-derived root from Rust (e.g. FUTO_NOTES_DATA_DIR for dev/test isolation). | **Fast** | Planned focused test against the new durable store or pure seam. |
| `src/lib/platform/tauriPaths.test.ts > ensureDir > invokes plugin-fs mkdir recursively` | The web/desktop shell must ensure that invokes plugin-fs mkdir recursively. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `ImagePasteSaveTest.rapidPastesGetDistinctFilesAndDoNotClobber` | The Android shell must ensure that rapidPastesGetDistinctFilesAndDoNotClobber. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `ImagePasteSaveTest.createsVaultDirIfMissing` | The Android shell must ensure that createsVaultDirIfMissing. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `ImagePasteSaveTest.rejectsDisallowedExtension` | The Android shell must ensure that rejectsDisallowedExtension. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `ImagePasteSaveTest.savesBytesAndReturnsFilename` | The Android shell must ensure that savesBytesAndReturnsFilename. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `FolderDeleteToastTest.pluralForZeroMovedNotes` | The Android shell must ensure that pluralForZeroMovedNotes. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `FolderDeleteToastTest.pluralForManyMovedNotes` | The Android shell must ensure that pluralForManyMovedNotes. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `FolderDeleteToastTest.singularForOneMovedNote` | The Android shell must ensure that singularForOneMovedNote. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `EditorLifecycleFlushTest.sameNoteIdCoalescesToLastRegisteredProvider` | The editor flush registry keeps only the newest provider for a note. | **Excluded** | Editor ownership and lifecycle behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.releaseRemovesOnlyItsOwnEntry` | Releasing an editor removes its own flush registration, not a newer editor's registration. | **Excluded** | Editor ownership and lifecycle behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.lastEditorReleaseClearsTheRegister` | Releasing the final editor empties the flush registry. | **Excluded** | Editor ownership and lifecycle behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.derivationIsDirtyWhenContentDivergesFromSaved` | An editor draft is dirty when its current content differs from its saved content. | **Excluded** | Editor dirty-state behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.derivationReKeysToTheLiveNoteIdAfterRename` | An editor's flush registration follows the live note ID after a rename. | **Excluded** | Editor ownership and lifecycle behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.cleanOrClosedEditorFlushesNothing` | Clean or closed editors do not issue background writes. | **Excluded** | Editor lifecycle behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.flushPullsOwnersDerivedDraft` | A background flush reads the owning editor's latest derived draft. | **Excluded** | Editor draft ownership is outside this rewrite. |
| `EditorLifecycleFlushTest.flushSeesTheStateAtFlushTimeNotAtRegistration` | A background flush uses state from flush time rather than stale registration-time state. | **Excluded** | Editor draft ownership is outside this rewrite. |
| `EditorLifecycleFlushTest.derivationIsNullWhenCleanOrNotLoaded` | A clean or unloaded editor has no draft to flush. | **Excluded** | Editor dirty-state behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.crossFadeFlushesOutgoingDirtyEditor` | Transitioning between editors flushes the outgoing dirty editor. | **Excluded** | Editor transition behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.renameFlushKeepsMidWriteKeystrokeDirty` | A keystroke made during a rename flush remains dirty afterward. | **Excluded** | Editor concurrency behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.claimIssuesDistinctTokens` | Each editor ownership claim receives a distinct token. | **Excluded** | Editor ownership behavior is outside this rewrite. |
| `EditorLifecycleFlushTest.renameFlushBugWouldMarkMidWriteKeystrokeAsSaved` | The known rename-flush race would incorrectly mark a concurrent keystroke as saved. | **Excluded** | Editor concurrency behavior is outside this rewrite. |
| `NoteListRepinTest.mostRecentlyModifiedFirst` | The Android shell must ensure that mostRecentlyModifiedFirst. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.idAscendingBreaksModifiedTies` | The Android shell must ensure that idAscendingBreaksModifiedTies. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.exactTopIsAtTop` | The Android shell must ensure that exactTopIsAtTop. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.scrolledPastSlopIsNotTop` | The Android shell must ensure that scrolledPastSlopIsNotTop. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.deepScrollIsNotTopEvenAtZeroOffset` | The Android shell must ensure that deepScrollIsNotTopEvenAtZeroOffset. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.resortOfAlreadySortedListIsStable` | The Android shell must ensure that resortOfAlreadySortedListIsStable. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NoteListRepinTest.subRowSettleSlopStillCountsAsTop` | The Android shell must ensure that subRowSettleSlopStillCountsAsTop. | **Acceptance** | Existing browser/native boundary suite, migrated to the new public operation. |
| `NotesStorageTest.existingInstallIsGrandfatheredOnInternal` | The Android shell must ensure that existingInstallIsGrandfatheredOnInternal. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.garbageSavedModeFallsThroughToDetection` | The Android shell must ensure that garbageSavedModeFallsThroughToDetection. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.emptyOrMissingDirIsNotAnExistingVault` | The Android shell must ensure that emptyOrMissingDirIsNotAnExistingVault. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.deviceFolderNameSeparatesDevFromProd` | The Android shell must ensure that deviceFolderNameSeparatesDevFromProd. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.freshInstallShowsPickerWhenDeviceSupported` | The Android shell must ensure that freshInstallShowsPickerWhenDeviceSupported. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.savedModeWins` | The Android shell must ensure that savedModeWins. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.pickerCoercesUnsupportedDeviceModeToAppStorage` | The Android shell must ensure that pickerCoercesUnsupportedDeviceModeToAppStorage. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.migrateIsIdempotentWhenRerun` | The Android shell must ensure that migrateIsIdempotentWhenRerun. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.migrateCopiesDotfilesAndImagesThenDeletesSource` | The Android shell must ensure that migrateCopiesDotfilesAndImagesThenDeletesSource. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.freshInstallPreAndroid11DefaultsToAppNoPicker` | The Android shell must ensure that freshInstallPreAndroid11DefaultsToAppNoPicker. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.migrateNoOpsOnEmptyOrSamePath` | The Android shell must ensure that migrateNoOpsOnEmptyOrSamePath. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.dirWithContentIsAnExistingVault` | The Android shell must ensure that dirWithContentIsAnExistingVault. | **Fast** | Planned focused test against the new durable store or pure seam. |
| `NotesStorageTest.pickerDefaultsToDeviceForLegacyInternalOnlyWhenDeviceSupported` | The Android shell must ensure that pickerDefaultsToDeviceForLegacyInternalOnlyWhenDeviceSupported. | **Fast** | Planned focused test against the new durable store or pure seam. |

## Follow-up queue

- Add an explicit crash/fault-injection seam for failures between collision parking, final rename, and backup cleanup; do not claim crash-window closure from happy-path atomic-write tests.
- Replace ignored size/performance probes with a repeatable 5k-note scan/reconcile benchmark after the new owner exists.
- Add a native iOS test target for note-store projection behavior; until then, Rust contracts plus a full Swift build and simulator smoke are the executable evidence.
- Keep sync-specific ingress healing and object-map behavior in the sync rewrite ledger; this rewrite may preserve the `futo-notes-core::files` callable contract for that consumer but will not change sync policy.

## Verification matrix

| Boundary | Executed evidence | Result |
| --- | --- | --- |
| Legacy-test translation | 481 legacy tests inventoried, translated to plain English, and dispositioned before production implementation | **Pass** |
| Durable Rust contracts | `cargo test -p futo-notes-core -p futo-notes-model -p futo-notes-store --lib --tests`: 225 passed, 3 ignored (including 18 adversarial filesystem tests and 11 new store-contract tests) | **Pass** |
| Desktop and native Rust projections | `cargo check -p futo-notes-ffi` and Tauri manifest `cargo check` | **Pass** |
| TypeScript note/folder/search coordination | Full Vitest: 802 passed, 10 skipped; all rewritten note contracts pass. The same six pre-existing `crashHandler.test.ts` cases fail because this Node runtime provides no `localStorage`; they are outside this rewrite and were present at baseline. | **Scoped pass; unchanged baseline exception** |
| TypeScript build | `tsc --noEmit` and production Vite build (3,989 modules transformed) | **Pass** |
| Public browser shell | Full Playwright acceptance suite: 419 passed, including folder create/rename/delete-with-move, full reset, search, note selection without a save, remote rename projection, and wikilink navigation/autocomplete | **Pass** |
| Cross-language and architecture gates | Conformance generation check, bridge-spec check, command reachability, platform discipline, drift registry, and debt ratchet | **Pass** |
| Android adapter | Rust FFI rebuilt for `arm64-v8a`, `armeabi-v7a`, and `x86_64`; Gradle unit target reports 64 tests, 0 skipped/failures/errors; debug APK assembled | **Pass** |
| iOS adapter | Rust/UniFFI XCFramework contains universal simulator `arm64` + `x86_64`; XcodeGen project generation and generic iOS Simulator Debug build | **Pass** |

Verification ran against the rewrite branch rooted at the recorded current `origin/main` commit (`8822da1abd202d907a0fa5410f5aba41b9ad8617`). No publication, commit, push, or merge request was performed.
