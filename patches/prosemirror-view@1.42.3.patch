diff --git a/dist/index.cjs b/dist/index.cjs
index 6ee53e1db4c61e2eeca5617e84cb16ce8b4730a0..5f5ae009b6c8a25aab04d7f4e67df319b1bb5d68 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -663,6 +663,8 @@ var ViewDesc = function () {
     this.dom = dom;
     this.contentDOM = contentDOM;
     this.dirty = NOT_DIRTY;
+    this.blockPosHint = null;
+    this.blockPos = 0;
     dom.pmViewDesc = this;
   }
   _createClass(ViewDesc, [{
@@ -717,6 +719,7 @@ var ViewDesc = function () {
   }, {
     key: "posBeforeChild",
     value: function posBeforeChild(child) {
+      if (child == this.blockPosHint) return this.blockPos;
       for (var i = 0, pos = this.posAtStart;; i++) {
         var cur = this.children[i];
         if (cur == child) return pos;
@@ -1416,12 +1419,31 @@ var NodeViewDesc = function (_ViewDesc4) {
   }, {
     key: "updateInner",
     value: function updateInner(node, outerDeco, innerDeco, view) {
+      var reuseBlocks = !this.parent && this.dirty < CONTENT_DIRTY && !view.composing && this.innerDeco == DecorationSet.empty && innerDeco == DecorationSet.empty && this.children.length == node.childCount;
       this.updateOuterDeco(outerDeco);
       this.node = node;
       this.innerDeco = innerDeco;
-      if (this.contentDOM) this.updateChildren(view, this.posAtStart);
+      if (this.contentDOM && !(reuseBlocks && this.updateBlockChildren(view))) this.updateChildren(view, this.posAtStart);
       this.dirty = NOT_DIRTY;
     }
+  }, {
+    key: "updateBlockChildren",
+    value: function updateBlockChildren(view) {
+      for (var i = 0, pos = this.posAtStart; i < this.children.length; i++) {
+        var desc = this.children[i], node = this.node.child(i);
+        if (!(desc instanceof NodeViewDesc) || node.marks.length || desc.outerDeco.length) return false;
+        if (desc.node != node || desc.dirty != NOT_DIRTY) {
+          var dom = desc.dom;
+          this.blockPosHint = desc;
+          this.blockPos = pos;
+          var updated = desc.update(node, [], DecorationSet.empty, view) && desc.dom == dom;
+          this.blockPosHint = null;
+          if (!updated) return false;
+        }
+        pos += node.nodeSize;
+      }
+      return true;
+    }
   }, {
     key: "updateOuterDeco",
     value: function updateOuterDeco(outerDeco) {
@@ -5165,7 +5187,7 @@ var EditorView = function () {
         this.domObserver.stop();
         var forceSelUpdate = updateDoc && (ie || chrome) && !this.composing && !prev.selection.empty && !state.selection.empty && selectionContextChanged(prev.selection, state.selection);
         if (updateDoc) {
-          var chromeKludge = chrome ? this.trackWrites = this.domSelectionRange().focusNode : null;
+          var chromeKludge = chrome && (!this.editable || this.hasFocus()) ? this.trackWrites = this.domSelectionRange().focusNode : null;
           if (this.composing) this.input.compositionNode = findCompositionNode(this);
           if (redraw || !this.docView.update(state.doc, outerDeco, innerDeco, this)) {
             this.docView.updateOuterDeco(outerDeco);
diff --git a/dist/index.js b/dist/index.js
index 012237dd813a21d86d6d527263b49c5af0a0e2f0..ef4d027c3bb46abe3c822132fe07318efd8a8a00 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -751,6 +751,10 @@ class ViewDesc {
         this.dom = dom;
         this.contentDOM = contentDOM;
         this.dirty = NOT_DIRTY;
+        // Set by updateBlockChildren around one child's update: the position
+        // before that child, so its posAtStart does not re-sum every sibling.
+        this.blockPosHint = null;
+        this.blockPos = 0;
         // An expando property on the DOM node provides a link back to its
         // description.
         dom.pmViewDesc = this;
@@ -786,6 +790,8 @@ class ViewDesc {
             this.children[i].destroy();
     }
     posBeforeChild(child) {
+        if (child == this.blockPosHint)
+            return this.blockPos;
         for (let i = 0, pos = this.posAtStart;; i++) {
             let cur = this.children[i];
             if (cur == child)
@@ -1476,13 +1482,38 @@ class NodeViewDesc extends ViewDesc {
         return true;
     }
     updateInner(node, outerDeco, innerDeco, view) {
+        // Reuse undecorated blocks on text edits; retain the general matcher for
+        // structural changes, decorations, and composition (src/viewdesc.ts).
+        let reuseBlocks = !this.parent && this.dirty < CONTENT_DIRTY && !view.composing &&
+            this.innerDeco == DecorationSet.empty && innerDeco == DecorationSet.empty &&
+            this.children.length == node.childCount;
         this.updateOuterDeco(outerDeco);
         this.node = node;
         this.innerDeco = innerDeco;
-        if (this.contentDOM)
+        if (this.contentDOM && !(reuseBlocks && this.updateBlockChildren(view)))
             this.updateChildren(view, this.posAtStart);
         this.dirty = NOT_DIRTY;
     }
+    updateBlockChildren(view) {
+        for (let i = 0, pos = this.posAtStart; i < this.children.length; i++) {
+            let desc = this.children[i], node = this.node.child(i);
+            if (!(desc instanceof NodeViewDesc) || node.marks.length || desc.outerDeco.length)
+                return false;
+            if (desc.node != node || desc.dirty != NOT_DIRTY) {
+                let dom = desc.dom;
+                // Every earlier child already matches its node, so pos is where
+                // this block starts; hand it to the update instead of re-deriving it.
+                this.blockPosHint = desc;
+                this.blockPos = pos;
+                let updated = desc.update(node, [], DecorationSet.empty, view) && desc.dom == dom;
+                this.blockPosHint = null;
+                if (!updated)
+                    return false;
+            }
+            pos += node.nodeSize;
+        }
+        return true;
+    }
     updateOuterDeco(outerDeco) {
         if (sameOuterDeco(outerDeco, this.outerDeco))
             return;
@@ -5583,7 +5614,8 @@ class EditorView {
                 // Chrome sometimes starts misreporting the selection, so this
                 // tracks that and forces a selection reset when our update
                 // did write to the node.
-                let chromeKludge = chrome ? (this.trackWrites = this.domSelectionRange().focusNode) : null;
+                // An unfocused editable does not own the DOM selection.
+                let chromeKludge = chrome && (!this.editable || this.hasFocus()) ? (this.trackWrites = this.domSelectionRange().focusNode) : null;
                 if (this.composing)
                     this.input.compositionNode = findCompositionNode(this);
                 if (redraw || !this.docView.update(state.doc, outerDeco, innerDeco, this)) {
diff --git a/src/index.ts b/src/index.ts
index 54f4d0eccf9ba6845984e32a148c537d1c203ca5..4105f198c36c89c21df68c3edcdb739ae74e160f 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -195,7 +195,11 @@ export class EditorView {
         // Chrome sometimes starts misreporting the selection, so this
         // tracks that and forces a selection reset when our update
         // did write to the node.
-        let chromeKludge = browser.chrome ? (this.trackWrites = this.domSelectionRange().focusNode) : null
+        // selectionToDOM does not own an unfocused editable's DOM selection.
+        // Reading focusNode here can nevertheless force layout of the whole
+        // document, especially with content-visibility on offscreen blocks.
+        let chromeKludge = browser.chrome && (!this.editable || this.hasFocus())
+            ? (this.trackWrites = this.domSelectionRange().focusNode) : null
         if (this.composing) this.input.compositionNode = findCompositionNode(this)
         if (redraw || !this.docView.update(state.doc, outerDeco, innerDeco, this)) {
           this.docView.updateOuterDeco(outerDeco)
diff --git a/src/viewdesc.ts b/src/viewdesc.ts
index 18a0caa29fd2ed9ac3b90ab4653dd86c025ac9b2..482381ca9708160a862850da7f867dd0777e535a 100644
--- a/src/viewdesc.ts
+++ b/src/viewdesc.ts
@@ -3,7 +3,7 @@ import {TextSelection} from "prosemirror-state"
 
 import {domIndex, isEquivalentPosition, DOMNode} from "./dom"
 import * as browser from "./browser"
-import {Decoration, DecorationSource, WidgetConstructor, WidgetType, NodeType} from "./decoration"
+import {Decoration, DecorationSet, DecorationSource, WidgetConstructor, WidgetType, NodeType} from "./decoration"
 import {EditorView} from "./index"
 
 declare global {
@@ -142,6 +142,11 @@ const NOT_DIRTY = 0, CHILD_DIRTY = 1, CONTENT_DIRTY = 2, NODE_DIRTY = 3
 // basic structure and shared methods.
 export class ViewDesc {
   dirty = NOT_DIRTY
+  // Set by updateBlockChildren around one child's update: the position before
+  // that child, already known from its walk, so the child's posAtStart does not
+  // re-sum every earlier sibling's size (O(blocks), twice for a nested edit).
+  blockPosHint: ViewDesc | null = null
+  blockPos = 0
   declare node: Node | null
 
   constructor(
@@ -192,6 +197,7 @@ export class ViewDesc {
   }
 
   posBeforeChild(child: ViewDesc): number {
+    if (child == this.blockPosHint) return this.blockPos
     for (let i = 0, pos = this.posAtStart;; i++) {
       let cur = this.children[i]
       if (cur == child) return pos
@@ -874,13 +880,40 @@ export class NodeViewDesc extends ViewDesc {
   }
 
   updateInner(node: Node, outerDeco: readonly Decoration[], innerDeco: DecorationSource, view: EditorView) {
+    // A text edit in an undecorated document does not need the general tree
+    // matcher (including its suffix map) for every unchanged top-level block.
+    // Structural changes, decorations, and IME composition use the full path.
+    let reuseBlocks = !this.parent && this.dirty < CONTENT_DIRTY && !view.composing &&
+        this.innerDeco == DecorationSet.empty && innerDeco == DecorationSet.empty &&
+        this.children.length == node.childCount
     this.updateOuterDeco(outerDeco)
     this.node = node
     this.innerDeco = innerDeco
-    if (this.contentDOM) this.updateChildren(view, this.posAtStart)
+    if (this.contentDOM && !(reuseBlocks && this.updateBlockChildren(view)))
+      this.updateChildren(view, this.posAtStart)
     this.dirty = NOT_DIRTY
   }
 
+  updateBlockChildren(view: EditorView): boolean {
+    for (let i = 0, pos = this.posAtStart; i < this.children.length; i++) {
+      let desc = this.children[i], node = this.node.child(i)
+      if (!(desc instanceof NodeViewDesc) || node.marks.length || desc.outerDeco.length)
+        return false
+      if (desc.node != node || desc.dirty != NOT_DIRTY) {
+        let dom = desc.dom
+        // Every earlier child already matches its node, so pos is where this
+        // block starts; hand it to the update instead of re-deriving it.
+        this.blockPosHint = desc
+        this.blockPos = pos
+        let updated = desc.update(node, [], DecorationSet.empty, view) && desc.dom == dom
+        this.blockPosHint = null
+        if (!updated) return false
+      }
+      pos += node.nodeSize
+    }
+    return true
+  }
+
   updateOuterDeco(outerDeco: readonly Decoration[]) {
     if (sameOuterDeco(outerDeco, this.outerDeco)) return
     let needsWrap = this.nodeDOM.nodeType != 1
