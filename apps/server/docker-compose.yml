services:
  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - data:/app/apps/server/data
    env_file:
      - .env

volumes:
  data:
