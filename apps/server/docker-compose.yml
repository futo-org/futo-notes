services:
  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    ports:
      - "${PORT:-3005}:3005"
    volumes:
      - data:/app/apps/server/data
    env_file:
      - .env
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-info}

volumes:
  data:
