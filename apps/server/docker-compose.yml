services:
  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    ports:
      - "${PORT:-3005}:3005"
    volumes:
      - data:/app/apps/server/data
    env_file:
      - .env

volumes:
  data:
