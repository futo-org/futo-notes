services:
  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    ports:
      - "${PORT:-3005}:${PORT:-3005}"
    volumes:
      - ${STONEFRUIT_DATA_PATH:-./data}:/app/data
    env_file:
      - .env
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-info}
